# تحسين التحميل الأولي لمكتبات التصدير — 25 أغسطس 2026

تم نقل مكتبات الاستخدام المتأخر في `SouqJiranApp.jsx` إلى `import()` كسول:

| المكتبة | متى تُحمّل الآن | الوظيفة المحفوظة |
| --- | --- | --- |
| `papaparse` | عند اختيار ملف CSV في استيراد المنتجات | معاينة واستيراد منتجات التاجر |
| `qrcode` | عند فتح بطاقة إحالة أو QR تاجر أو QR موصل | إنشاء رمز QR ديناميكي |
| `jspdf` | عند تنزيل ملصق متجر PDF | ملصق A4 مع خط Amiri العربي وRTL |

أظهر بناء الإنتاج الناتج أن أجزاء `papaparse` (18.86 كيلوبايت) و`jspdf` (386.19 كيلوبايت) أصبحت ملفات منفصلة تُحمّل عند الحاجة. بقيت الحزمة الرئيسية 1.65 ميغابايت مصغرة، لأنها ما زالت تضم واجهة التطبيق الأحادية الكبيرة وبياناتها؛ لا يُعد هذا التغيير دليلاً على نجاح تنزيل PDF أو QR في جلسة مستخدم حية، لكنه يحافظ على التغطية الاختبارية والبناء.

## Firebase عند الحاجة

تمت إزالة الاستيراد المتزامن لمساعدة Firebase من الواجهة. تُحمّل `client/src/lib/firebase.ts` الآن فقط عند ضغط المستخدم على تعبئة Google أو عند وجود جلسة تتطلب مزامنة FCM أو الاستماع لإشعار أصلي. بقيت طلبات الأذونات وتدفقات Google كما هي، وأضيف اختبار يمنع عودة الاستيراد المتزامن.

أظهر بناء الإنتاج جزء Firebase منفصلاً بحجم **66.10 كيلوبايت** مصغراً (**21.16 كيلوبايت gzip**)، وانخفضت الحزمة الرئيسية إلى **1.59 ميغابايت** مصغرة (**387.14 كيلوبايت gzip**). لا يزال فصل واجهة التطبيق الأحادية الكبيرة إلى مكونات مستقلة هو التحسين التالي الممكن للحزمة الرئيسية.

## فصل الحزم المستقرة للتخزين المؤقت

أضيفت إعدادات `manualChunks` في `vite.config.ts` لفصل اعتماديات React وSupabase وFirebase ومكتبة الأيقونات عن حزمة واجهة التطبيق. يتيح ذلك للمتصفح إعادة استخدام هذه الأجزاء عند نشر تعديل في الواجهة لا يمس الاعتماديات.

| الجزء الناتج من بناء الإنتاج | الحجم المصغّر | الحجم بعد gzip |
| --- | ---: | ---: |
| واجهة التطبيق `index` | 955.72 كيلوبايت | 204.00 كيلوبايت |
| React runtime | 395.68 كيلوبايت | 116.85 كيلوبايت |
| Supabase client | 216.82 كيلوبايت | 57.10 كيلوبايت |
| Firebase platform | 160.60 كيلوبايت | 47.14 كيلوبايت |
| مكتبة الأيقونات | 16.96 كيلوبايت | 5.66 كيلوبايت |
| jsPDF المحمّل عند الحاجة | 386.30 كيلوبايت | 126.46 كيلوبايت |

التحقق المحلي بعد هذه القياسات: 75 اختبار Vitest ناجحاً، وبناء إنتاج ناجح، و`git diff --check` بلا أخطاء. ما زال Vite يعرض تحذير الحجم القياسي لحزمة الواجهة التي تتجاوز 500 كيلوبايت؛ وهو ليس فشل بناء، ولا يستدعي تفكيكاً واسعاً للمكوّن الأحادي ما لم تظهر حاجة أداء مثبتة.
