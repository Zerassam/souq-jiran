# تحقق إعدادات المصادقة العامة — 25 أغسطس 2026

تم إجراء تحقق غير متلف على نقطة إعدادات Supabase العامة مع المفتاح القابل للنشر فقط. لم يُرسل أي رمز بريد ولم تبدأ أي جلسة مستخدم.

| المسار | النتيجة | نطاق التحقق |
| --- | --- | --- |
| Email OTP | جاهز | تعلن إعدادات Supabase العامة تفعيل مزود البريد. |
| تأكيد البريد | لا يمكن إثباته آلياً هنا | يلزم إرسال رمز إلى صندوق بريد حقيقي وإدخاله، ولم يُنفذ ذلك. |
| Google في Supabase | غير مفعّل | هذا متوقع في هذا المشروع؛ لا يستخدم التطبيق مزود Google من Supabase. |
| Google في التطبيق | محروس في الاختبارات | يستخدم التدفق `requestGoogleProfilePrefill` عبر Firebase Authentication لتعبئة الاسم والبريد، وتغطيه اختبارات الحراسة. |

يبقى نجاح Google وEmail OTP على جهاز Android وحساب حقيقي ضمن اختبار القبول اليدوي فقط.
