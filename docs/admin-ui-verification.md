# سجل التحقق من واجهة الإدارة

- تم تسجيل الدخول إلى واجهة سوق الجيران بالحساب `listportail@gmail.com` بعد ترقية ملفه إلى الدور `admin` في Supabase.
- ظهرت لوحة **إدارة طلبات الانضمام**، وعرضت عند الفحص خمس طلبات محلات وثلاث طلبات موصلين قيد المراجعة.
- بعد موافقة صريحة من مالك المشروع، تم اعتماد حساب التاجر الاختباري `UI Merchant 1787000381467` وحساب موصل الدراجة `bicycle · Alger Centre` من الواجهة.
- تُستخدم هذه النتيجة لإثبات أن واجهة الإدارة تقرأ الطلبات المعلّقة وأن إجراء الاعتماد مرتبط بدور `admin`.

## تحقق التجارة ولوحة الإدارة — 17 أغسطس 2026

تم تشغيل اختبار واجهة حي عبر `scripts/verify-supabase-commerce-ui-flow.mjs` على المعاينة المحلية المتصلة بـ Supabase. سجّل الاختبار دخول التاجر المعتمد `UI Merchant 1787000381467`، وأنشأ المنتج `منتج تحقق Supabase 1787008030613` بسعر 100 دج، ثم أنشأ حساب عميل اختبار جديد. ظهر المنتج في كتالوج المحل، أُضيف إلى السلة، وأُرسل الطلب نقداً عند الاستلام. بعد إعادة دخول التاجر، ظهر الطلب في لوحة الطلبات الواردة، وتم تغيير حالته بنجاح إلى «تم القبول» عبر الإجراء المحمي.

أُزيل أيضاً زر «تأكيد» قديم من جدول متابعة طلبات الإدارة لأنه كان يستدعي معالجاً غير معرّف ولا يقابل عملية محفوظة في Supabase. بعد الإصلاح، أُعيد فتح لوحة الإدارة في المتصفح وفُحص قسم «متابعة الطلبات المباشرة»؛ ظهر الجدول بأعمدة المحل والعميل والمبلغ والحالة دون أي خطأ runtime في وحدة تحكم المتصفح. يغطي اختبار Vitest تلقائياً عدم عودة الاستدعاء `confirmOrder` إلى مصدر الواجهة.

## تحقق الأرشفة الإدارية — 17 أغسطس 2026

بعد تطبيق الترحيل `20260817_admin_archives.sql` في Supabase، أُنشئ طلب اختبار بالمنتج `منتج أرشفة Supabase 1787010354737` من خلال واجهة التجارة، ثم أُرشف من حساب التاجر عبر زر الحذف الشخصي. اختفى السجل من واجهة التاجر فقط. بعد دخول المشرف، ظهر السجل نفسه في قسم **أرشيف الطلبات الكامل** باسم التاجر والعميل، وتفاصيل المنتج والمبلغ (101 دج)، وحالته «قيد الانتظار»؛ وبذلك تأكد أن الأرشفة لا تمس النسخة الإدارية الأصلية. كما ظهر زر **حذف نهائي** في الأرشيف ولا ينفذ الإجراء إلا بعد تأكيد صريح من المشرف.

أثناء الفحص، عولجت مراجع أيقونتي `Archive` و`MessageCircle` غير المستوردتين في لوحة الإدارة، ثم أُعيد تحميل الواجهة بنجاح وظهر الأرشيف كاملاً من دون أخطاء runtime.

## تسليم التحديث إلى GitHub — 17 أغسطس 2026

بعد اجتياز اختبارات Vitest وبناء الإنتاج، حُفظت نقطة التحقق الخاصة بالأرشفة ثم رُفعت إلى المستودع [`Zerassam/souq-jiran`](https://github.com/Zerassam/souq-jiran) على الفرع `main`. نجح الرفع بالإيداع `3012db1`، وهو يتضمن ترحيل `20260817_admin_archives.sql` وواجهة الأرشفة وأرشيف المشرف وإصلاحات لوحة الإدارة.

## تحقق أدوات إدارة الأرشيف المتقدمة — 18 أغسطس 2026

بعد تطبيق الترحيل `20260818_archive_management.sql`، شُغّل سيناريو أرشفة حي لطلب اختبار مرتفع القيمة باسم `منتج أرشفة حساسة Supabase 1787012190519` وقيمة 5,001 دج. تجاوز الطلب معيار الحساسية المضبوط على 5,000 دج، فظهر في لوحة المشرف تنبيه **«أرشفة طلب حساس»** يحدد القيمة والحالة والفاعل. تم اختبار زر **«تأكيد القراءة»** من اللوحة، فتحولت حالة التنبيه إلى مقروء وأصبح عداد غير المقروءة محدثاً. كما تحقّق البحث النصي في الأرشيف والفلاتر حسب نوع السجل والحالة، مع بقاء سجل تدقيق الحذف النهائي مخصصاً للإجراءات الإدارية وحدها.

اكتمل تحقق الجودة بعد ذلك بنجاح: اجتازت 11 حالة في Vitest، بما فيها اختبارات الاتصال الحي وسياسات RLS والبنية الثابتة للواجهة، كما اكتمل بناء الإنتاج باستخدام `pnpm build`. يظهر تحذير حجم حزمة Vite فقط ولا يمنع البناء؛ يمكن معالجة تقسيم الحزمة لاحقاً كتحسين أداء مستقل.

## تحقق مراجعة حسابات الاختبار — 18 أغسطس 2026

بعد تنفيذ الترحيل `20260819_test_account_review.sql` في Supabase، سُجل الدخول إلى لوحة المشرف بالحساب المصرح له، وظهر قسم **«مراجعة حسابات الاختبار»** بنجاح. أكّد القسم أن المراجعة لا تعرض إلا الحسابات ذات وسم البريد التجريبي المطابق، وأن أي حساب يملك طلبات أو حالة تشغيلية معتمدة مستبعد من الحذف. كانت النتيجة الحية الحالية **0 مرشح**، مع ظهور رسالة واضحة تفيد بعدم وجود حسابات مؤهلة، ولم يُنفّذ أي حذف أثناء التحقق.
